# Done to increase the memory available to gradle.
org.gradle.jvmargs=-Xmx1G
org.gradle.parallel=true

# IntelliJ IDEA is not yet fully compatible with configuration cache, see: https://github.com/FabricMC/fabric-loom/issues/1349
org.gradle.configuration-cache=false

# Fabric Properties
# check these on https://fabricmc.net/develop
# Built against 26.3-snapshot-4; the code is source-compatible with
# 26.3-snapshot-3 as well (the only cushion API it uses is unchanged), and the
# fabric.mod.json depends on the whole 26.3 range.
minecraft_version=26.3-snapshot-4
loader_version=0.19.3
loom_version=1.17-SNAPSHOT

# Mod Properties
mod_version=1.0.0
maven_group=dev.tggamesyt

# Dependencies
fabric_api_version=0.155.2+26.3
